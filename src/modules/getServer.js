

// Django and react must on same host (localhost) in dev

export default function getServer(){
  const localhost  = "http://localhost:8000/";
  const production = "https://www.sketchdrawdoodle.com";

  return localhost
}